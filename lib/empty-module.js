// Empty shim for Node-only modules that roslib references but never uses in React Native.
// React Native provides native WebSocket, so roslib's ws/stream/http paths are dead code.
module.exports = {};
